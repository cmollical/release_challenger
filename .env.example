# Server-side only. Add these in Vercel Project Settings → Environment Variables.
OPENAI_API_KEY=your_openai_api_key
# Optional; defaults to gpt-4.1-mini in the server route.
OPENAI_MODEL=gpt-4.1-mini
